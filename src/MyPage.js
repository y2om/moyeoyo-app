// MyPage.js는 더 이상 사용되지 않습니다.
// 삭제 요청에 따라 컴포넌트 기능을 제거하였습니다.

const MyPage = () => {
    return (
        <div style={{ padding: "2rem" }}>
            <h2>마이페이지 기능은 현재 비활성화되어 있습니다.</h2>
        </div>
    );
};

export default MyPage;
